// Varuiables y constantes

// 'var' no usarlo

const name = 'Juan';  // 'const' Si el valor no va a cambiar
const lastName ='Santana';

let age = 5; // 'let' Si el valor va a cambiar
age = 25;

console.log( name, lastName, age);

if(true) {
    const name = 'David';

    console.log(name);
}

console.log( name, lastName, age);